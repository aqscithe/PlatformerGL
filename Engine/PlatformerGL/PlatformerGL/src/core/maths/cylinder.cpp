#include "core/maths/cylinder.hpp"

Core::Maths::Cylinder::Cylinder(const vec3& center, const float& height, const float& radius, const Quaternion& q)
	:center(center), height(height), radius(radius), q(q)
{

}